apiVersion: v1
kind: Service
metadata:
  name: cadvisor
  namespace: xconnected
spec:
  type: LoadBalancer
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
  selector:
    k8s-app: cadvisor  # Fixed selector to match DaemonSet labels
