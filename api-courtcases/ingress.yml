apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: courtcases-api-ingress
  namespace: xconnected
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
    # traefik.ingress.kubernetes.io/router.rule: "PathPrefix(`/api-config`,`/static`)"
spec:
  rules:
  - 
    http:
      paths:
      - path: /api-courtcases
        pathType: Prefix
        backend:
          service:
            name: courtcases-api
            port:
              number: 8004
      - path: /static
        pathType: Prefix
        backend:
          service:
            name: courtcases-api
            port:
              number: 8004