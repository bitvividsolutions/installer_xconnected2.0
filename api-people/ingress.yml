apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api-people-ingress
  namespace: xconnected
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  rules:
  - http:
      paths:
      - path: /api-people
        pathType: Prefix
        backend:
          service:
            name: people-api-service
            port:
              number: 8005