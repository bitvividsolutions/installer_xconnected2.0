apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongodb-pvc
  namespace: xconnected
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi 
  storageClassName: manual
